[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.6"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.3", features = ["compression-gzip", "fs"] }
hyper = "0.14"

[lints]
workspace = true
